# Math-Issue Tracker (Phase 2)

One row per fix: location, original claim, why false/too strong, patch, downstream dependencies, status.

| ID | Location | Original claim | Why false / too strong | Patch | Downstream deps | Status |
|---:|---|---|---|---|---|---|
| 11 | `sections/lqg_constructive_solution.tex` (Prop.~`prop:lqg_constructive`) | Fixed-point space `L^\infty_{\mathcal F^0}(\Omega\times[0,T])` with `\|\cdot\|_\infty`. | Too strong for Brownian-driven objects; downstream convergence is mean-square / `L^2`. | Switch to `\mathcal X := L^2_{\mathcal F^0}(\Omega\times[0,T])`, norm `\|\cdot\|_2 := (\E\int_0^T |\cdot|^2 dt)^{1/2}`; rewrite Lipschitz steps in `L^2`. | Stability summary in Section IV; Picard residual text. | ✅ |
| 12–13 | `sections/lqg_constructive_solution.tex` (Prop.~`prop:lqg_constructive`, Step 1) | “If `\widetilde A_s<0` then `\dot{\widetilde A}_s \ge \kappa^2/\sigma_\eta^2`” used to deduce `A\ge 0`. | Inequality is invalid because `-\beta^2 \widetilde A^2 \le 0` so `\dot{\widetilde A}_s \le \kappa^2/\sigma_\eta^2` always. | Replace with invariant-region / barrier argument: uniqueness + positive drift at 0 implies cannot cross into negatives. | Bounds for `A` used in contraction constant `q`. | ✅ |
| 27 | `sections/appx.tex` (Lemma `lem:riccati_convergence`) | `|\Sigma_t-\Sigma^*|\le 2\Sigma^*|\rho|e^{-2at}` from `\Sigma_t-\Sigma^*=-2\Sigma^*\rho e^{-2at}/(1+\rho e^{-2at})`. | Fails for `\rho<0` (`\Sigma_0>\Sigma^*`) because dividing by `1+\rho e^{-2at}\in(0,1)` increases magnitude. Also the derivation uses `\operatorname{artanh}(\Sigma_0/\Sigma^*)` outside its real domain. | Keep exact identity; bound with a correct denominator lower bound (piecewise by sign of `\rho`). Replace the derivation with a ratio ODE (no `\operatorname{artanh}` domain issue). | Any place citing exponential convergence / constant-gain approximation. | ✅ |
| 5,16 | `sections/sec4_results.tex` (Prop.~`prop:tracking_error`) | “trade-off (not necessarily monotone in `k`)” for `\Var(e_\infty)` under the current `R'(k)=\sigma_c^2+\sigma_\epsilon^2/k`. | With current definitions, `K^*(k)` is increasing and (if `\sigma_c>0`) bounded above by `\sigma_v/\sigma_c`, and `\Var(e_\infty)` is decreasing over that range. | Replace narrative with correct monotonicity statement; add short derivative argument in proof. | Interpretation text in Section IV; numerics narrative. | ✅ |
| 8 | `sections/lqg_constructive_solution.tex` (innovation form display) | “`I^p` and `I^\xi` are independent Brownian motions” stated without specifying measure/filtration, despite misspecified filtering when `k\ne 1`. | Under misspecification, objective innovation independence need not hold; at minimum must be “under the agent’s subjective model”. | Weaken/qualify: innovations are Brownian/independent under the agent’s subjective filtering assumptions; no downstream proof uses objective independence. | None (contraction proof does not use it). | ✅ |

